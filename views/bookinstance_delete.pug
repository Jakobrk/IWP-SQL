extends layout

block content
    h1 Delete Bookinstance
    p #{book.title} - #{bookinstance.imprint}
   
    p Are you sure you want to delete this instance?

    form(method='POST' action='')
        input(type='hidden' name='bookinstanceid' value=bookinstance.id)
        button(type='submit') Delete


